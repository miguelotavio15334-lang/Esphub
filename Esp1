-- CIRILOMI HUB - FIX TELA PRETA - VERSÃO FINAL
repeat task.wait() until game:IsLoaded()
pcall(function() game.CoreGui.CiriloClownESP:Destroy() end)
pcall(function() gethui().CiriloClownESP:Destroy() end)

local gui = Instance.new("ScreenGui")
gui.Name = "CiriloClownESP"
gui.Parent = gethui and gethui() or game.CoreGui
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 270, 0, 380)
main.Position = UDim2.new(0.5, -135, 0.5, -190)
main.BackgroundColor3 = Color3.fromRGB(25,25,25)
main.BorderSizePixel = 0
Instance.new("UICorner", main).CornerRadius = UDim.new(0,10)
main.Active = true
main.Draggable = true

local top = Instance.new("Frame", main)
top.Size = UDim2.new(1,0,0,35)
top.BackgroundColor3 = Color3.fromRGB(130,0,255)
Instance.new("UICorner", top).CornerRadius = UDim.new(0,10)

local title = Instance.new("TextLabel", top)
title.Size = UDim2.new(1,0,1,0)
title.Text = "  CiriloMi - Auto Hop ROXO 🤡"
title.TextXAlignment = Left
title.TextColor3 = Color3.new(1,1,1)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 13

local scroll = Instance.new("ScrollingFrame", main)
scroll.Size = UDim2.new(1,0,1,-35)
scroll.Position = UDim2.new(0,0,0,35)
scroll.CanvasSize = UDim2.new(0,0,0,500)
scroll.ScrollBarThickness = 4
scroll.BackgroundTransparency = 1
scroll.BorderSizePixel = 0

local layout = Instance.new("UIListLayout", scroll)
layout.Padding = UDim.new(0,8)
layout.SortOrder = Enum.SortOrder.LayoutOrder

local pad = Instance.new("UIPadding", scroll)
pad.PaddingTop = UDim.new(0,10)
pad.PaddingLeft = UDim.new(0,10)
pad.PaddingRight = UDim.new(0,10)

local function makeBtn(text, color, callback)
    local b = Instance.new("TextButton", scroll)
    b.Size = UDim2.new(1,0,0,38)
    b.Text = text
    b.BackgroundColor3 = color
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 11
    Instance.new("UICorner", b).CornerRadius = UDim.new(0,8)
    b.MouseButton1Click:Connect(function()
        local r = callback()
        if r then b.Text = r end
    end)
    return b
end

getgenv().ESP = false
getgenv().RoubarNormal = false
getgenv().RoubarFast = false
getgenv().HopRoxo = false

local status = makeBtn("Status: Pronto", Color3.fromRGB(40,40,40), function() return "Status: Pronto" end)
status.AutoButtonColor = false

makeBtn("ESP PALHAÇO [OFF]", Color3.fromRGB(60,60,60), function()
    getgenv().ESP = not getgenv().ESP
    if getgenv().ESP then
        status.Text = "ESP: ON"
        return "ESP PALHAÇO [ON]"
    else
        status.Text = "ESP: OFF"
        return "ESP PALHAÇO [OFF]"
    end
end)

makeBtn("AUTO ROUBAR [NORMAL] - VERDE", Color3.fromRGB(0,170,0), function()
    getgenv().RoubarNormal = not getgenv().RoubarNormal
    if getgenv().RoubarNormal then getgenv().RoubarFast = false status.Text = "Roubar Normal: ON" return "AUTO ROUBAR [NORMAL] ON" 
    else status.Text = "Roubar: OFF" return "AUTO ROUBAR [NORMAL] - VERDE" end
end)

makeBtn("AUTO ROUBAR [RAPIDO 💔] - VERMELHO", Color3.fromRGB(200,0,0), function()
    getgenv().RoubarFast = not getgenv().RoubarFast
    if getgenv().RoubarFast then getgenv().RoubarNormal = false status.Text = "Roubar Rapido: ON" return "AUTO ROUBAR [RAPIDO] ON" 
    else status.Text = "Roubar: OFF" return "AUTO ROUBAR [RAPIDO 💔] - VERMELHO" end
end)

makeBtn("AUTO HOP ROXO [OFF] - SALVA TUDO", Color3.fromRGB(120,0,200), function()
    getgenv().HopRoxo = not getgenv().HopRoxo
    if getgenv().HopRoxo then
        status.Text = "HOP ROXO ON - Trocando..."
        return "AUTO HOP ROXO [ON] 💜"
    else
        status.Text = "Hop Roxo OFF"
        return "AUTO HOP ROXO [OFF] - SALVA TUDO"
    end
end)

makeBtn("TROCAR SERVER AGORA", Color3.fromRGB(80,80,80), function()
    local TPS = game:GetService("TeleportService")
    local Http = game:GetService("HttpService")
    local data = Http:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Asc&limit=100")).data
    for _,s in pairs(data) do
        if s.playing < s.maxPlayers and s.id ~= game.JobId then
            queue_on_teleport([[
                getgenv().HopRoxo = true
                getgenv().ESP = true
                loadstring(game:HttpGet("https://raw.githubusercontent.com/"))()
            ]])
            TPS:TeleportToPlaceInstance(game.PlaceId, s.id, game.Players.LocalPlayer)
            break
        end
    end
    return "TROCAR SERVER AGORA"
end)

local info = Instance.new("TextLabel", scroll)
info.Size = UDim2.new(1,0,0,110)
info.Text = "💜 COMO USA:\n1. Liga ESP\n2. Liga 1 modo de Roubar\n3. Liga HOP ROXO\nEle vai trocar sozinho e quando trocar já volta tudo ligado!"
info.TextWrapped = true
info.TextXAlignment = Left
info.TextYAlignment = Top
info.BackgroundTransparency = 1
info.TextColor3 = Color3.fromRGB(200,200,200)
info.Font = Enum.Font.Gotham
info.TextSize = 11

print("CiriloMi - FIX Tela Preta ON")
game.StarterGui:SetCore("SendNotification", {Title="CiriloMi Hub", Text="Hub carregado! Arraste pra mover", Duration=3})
